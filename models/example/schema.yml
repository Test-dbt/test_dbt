
version: 2

models:
    - name: my_first_dbt_model
      meta: 
        owner: "supratim"
      description: "Chanding description for the 2nd time"
      tests:
         - unique
         - not_null

      columns:
          - name: id
            description: "Adding column description. Checking, 1,2,3..."


    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

